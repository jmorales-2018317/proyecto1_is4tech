import { SharedDirective } from './whitespace.directive';

describe('SharedDirective', () => {
  it('should create an instance', () => {
    const directive = new SharedDirective();
    expect(directive).toBeTruthy();
  });
});
